import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tonkaew131.boston.app',
  appName: 'boston',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
